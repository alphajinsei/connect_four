# Connect Four 強化学習プロジェクト

## ユーザーの目標
強化学習（DQN）を勉強中。Connect Four（四目並べ）を題材に、AIが学習して強くなる過程を実感したい。
**最終目標: 初心者の人間より強くなる**

## 学習ロードマップ（現行）

### 現在の方針（ステージ7: 2026-03-17 開始）
**シンプルカリキュラム学習（NoisyRuleBased → 完全RuleBased）+ 小スケール中間報酬 + 遅延コールバック方式** ← Phase 1 から学習中

ステージ6からの変更点:
1. **Phase 1 目標勝率を 80% → 75% に引き下げ**: noise=0.8はほぼランダムなので、ここで足踏みする意味が薄い
2. **Phase 4 スナップショットプール方式を廃止**: 純粋に noise=0.1 のみに簡素化
3. **Phase 5 追加**: noise=0.0（完全ルールベースAI）、目標勝率40%

カリキュラム:
| フェーズ | 相手 | 目標勝率 |
|---|---|---|
| 1 | NoisyRuleBased(noise=0.8) | 75% |
| 2 | NoisyRuleBased(noise=0.5) | 70% |
| 3 | NoisyRuleBased(noise=0.2) | 60% |
| 4 | NoisyRuleBased(noise=0.1) | 50% |
| 5 | RuleBasedAgent(noise=0.0) | 40% |

- 昇格条件: vs Noisy 500戦 × 2回連続クリア（まぐれ昇格防止）
- Phase 5では完全ルールベースAI固定。過去（ステージ4）で失敗した理由は (1) next_stateバグ (2) 段階的強化なし の2つだったが、両方解消済み。崩壊が起きたら打ち切り判断可能。

### 過去の方針（破棄・理由は後述）
1. ~~**相手を固定**（ランダムエージェント）~~ 廃止
2. ~~**カリキュラム学習**（段階的に強い相手）~~ 廃止
3. ~~**Self-play + 混合学習**~~ 廃止
4. ~~**vs ルールベースAI固定（先手固定）**~~ 廃止
5. ~~**NoisyRuleBasedカリキュラム + 大スケール中間報酬 + 旧GameRunner**~~ 廃止（next_stateバグ + 中間報酬過大）
6. ~~**遅延コールバック + 小スケール中間報酬 + 目標80% + プール方式Phase4**~~ 廃止（目標高すぎ + プール不要）

---

## 実行環境
- Python仮想環境: `.venv/`（プロジェクトルートに配置）
- **Pythonの実行は必ず `.venv` を使うこと**
  - 実行: `.venv/Scripts/python`
  - pip: `.venv/Scripts/pip`
- インストール済みパッケージ: `numpy`, `flask`
- 学習速度: **約3エピソード/秒**（eval毎に200戦評価あり）

## ファイル構成

```
4moku/
├── env/
│   └── connect4_env.py         # ゲームロジック（重力・勝利判定・状態表現・攻防報酬）
├── agents/
│   ├── base_agent.py           # 抽象基底クラス
│   ├── human_agent.py          # 人間プレイヤー（WebUI経由）
│   ├── random_agent.py         # ランダムAI
│   ├── rule_based_agent.py     # ルールベースAI（勝ち手・阻止・中央優先）
│   ├── noisy_rule_based_agent.py  # ★新規: noise パラメータで強さを調整（カリキュラム用）
│   └── dqn_agent.py            # DQNエージェント（NumPy手書き実装）
├── ui/
│   └── cli_ui.py               # CLI表示（現在はWebUIがメイン）
├── web/
│   ├── app.py                  # Flask サーバー（weights があれば DQNAgent を自動ロード、CPU先攻・プレイヤー後攻）
│   └── templates/
│       └── index.html          # ブラウザUI（アニメーション付き）
├── weights/
│   ├── dqn_connect4.npz              # 学習済み重み（WebUI用、軽量）
│   ├── dqn_connect4_checkpoint.npz   # チェックポイント（バッファ+ε+Adam、学習再開用）
│   ├── train_log.txt                 # 学習ログ（リアルタイム書き出し）
│   ├── snapshots/                    # best_ep* + phaseup_ph*（重みのみ）
│   └── archive/                      # 過去の学習重み（参照用）
├── game_runner.py              # ターン管理（env・agent・UIを繋ぐ）
├── train.py                    # 学習スクリプト（現行: カリキュラム学習 + 攻防報酬）
├── play.py                     # CLIで人間 vs ランダムAI
└── CLAUDE.md                   # このファイル
```

## Webアプリの起動方法
```bash
cd c:\Users\peinn\OneDrive\sandbox\claude-sandbox\RL_ReinforcementLearning\4moku
.venv\Scripts\python web/app.py
# ブラウザで http://localhost:5000 を開く
```
- `weights/dqn_connect4.npz` があれば自動で DQNAgent を読み込む
- なければ RandomAgent にフォールバック
- **CPU（赤）が先攻（PLAYER1）、プレイヤー（橙）が後攻（PLAYER2）**
  - DQNはPLAYER1（先攻固定）として学習しているため、UIをそれに合わせている
  - ゲーム開始時にCPUが先に1手打ち、以降はプレイヤー → CPU の順でターンが回る

## 学習の実行（現行）
```bash
# 基本: カリキュラム学習でゼロから（推奨）
.venv\Scripts\python train.py --episodes 30000

# 学習済みチェックポイントから続き（バッファ+ε+Adam状態を引き継ぐ）
.venv\Scripts\python train.py --load-path weights/dqn_connect4 --episodes 30000

# バッファを引き継がずに再開（重み+ε+Adam状態のみ復元）
.venv\Scripts\python train.py --load-path weights/dqn_connect4 --no-buffer --episodes 30000
```
- フェーズ自動移行: vs Noisy 勝率が目標値を超えたら次のフェーズへ（コンソールに `[Phase Up]` と表示）
- 完了後 `weights/dqn_connect4.npz`（重み）と `weights/dqn_connect4_checkpoint.npz`（バッファ+学習状態）に保存
- 学習ログは `weights/train_log.txt` にリアルタイム書き出し
- vs RuleBased 勝率（200戦）が自己ベストを更新するたびにスナップショット保存（重みのみ、軽量）
- フェーズ昇格時にもスナップショット保存（`phaseup_ph*`）

---

## 設計の重要ポイント

### env/connect4_env.py
- `step()` は相手を呼び出さない（self-playやカリキュラム学習に対応するため）
- `get_state(perspective)` で視点を切り替えられる（DQN訓練・self-playで重要）
- 報酬は常に PLAYER1 視点で返し、`GameRunner` が各エージェント視点に変換する
- 状態は shape `(3, 6, 7)` の float32 配列
  - ch0: 自分のコマ
  - ch1: 相手のコマ
  - ch2: 自分のターンなら 1.0、そうでなければ 0.0

### agents/dqn_agent.py
- 入力: `3 × 6 × 7 = 126`、出力: `7`（列インデックス）
- アーキテクチャ: `Linear(126→256) → ReLU → Linear(256→256) → ReLU → Linear(256→7)`
- NumPyベース（PyTorch不使用）
- ε-greedy + 無効手マスキング、ReplayBuffer、ターゲットネットワーク付き
- `save()`/`load()`: 重みのみ（WebUI・スナップショットプール用、軽量）
- `save_checkpoint()`/`load_checkpoint()`: 重み + ReplayBuffer + ε + total_steps + Adam状態（学習再開用）
  - チェックポイントは `_checkpoint.npz` として別ファイル保存（`np.savez_compressed`で圧縮）
  - `load_checkpoint(path, load_buffer=False)` でバッファのみスキップ可能

### agents/rule_based_agent.py
- 学習なし・手書きルールで動く
- 優先順位: ①自分が即勝てる列 → ②相手が次に勝てる列を防ぐ → ③中央寄りをランダム選択
- ランダムAI相手の勝率: 先手99.5%、後手96%
- 現在のDQNの勝率: 未計測（カリキュラム学習で段階的に目指す）

### agents/noisy_rule_based_agent.py（★新規 2026-03-16）
- `RuleBasedAgent` を継承し、`noise` 確率でランダム手を混入
- `noise=0.0`: 完全ルールベースAI、`noise=1.0`: 完全ランダム
- カリキュラム学習の各フェーズで使用（`train.py` が自動切り替え）

### 現在のハイパーパラメータ（train.py）
| パラメータ | 値 | 備考 |
|---|---|---|
| `lr` | 5e-4 | 1e-3では崩壊が激しかったため下げた |
| `epsilon_end` | 0.10 | 0.05では固定相手への過学習が激しかったため上げた |
| `epsilon_decay` | 0.99990 | ~30000ステップ(≒3750ep)でε=0.10に到達 |
| `warmup_steps` | 2000 | バッファを十分蓄積してから学習開始 |
| `target_update_interval` | 500 | 200では不安定だったため増やした |
| `buffer_capacity` | 200000 | 多様な経験を保持（フェーズ間で自然に入れ替わる） |
| `batch_size` | 128 | 安定した勾配 |

### GameRunner の遅延コールバック方式（2026-03-17 導入）

**旧方式（廃止）:** 自分が打った直後に `on_step_end(s, a, r, s', done)` を呼んでいた。
- `s'` = 「自分が打った直後の盤面（次は相手のターン）」
- 問題: Q学習の理論的前提が崩れる。`s'` は相手のターンなので、自分がこの状態から行動するわけではない。
  `max Q(s',a')` の予測値は「自分のターンの状態」で学習しているため、「相手のターンの状態」を入力した予測は信頼できない。
- 対症療法として `r - γ*maxQ(s')` と符号反転していたが、本質的には不正確だった。

**現行方式:** `on_step_end` を「相手が1手打った後（再び自分のターン）」に遅延して呼ぶ。
- `s'` = 「相手が打った後、再び自分のターンの盤面」
- 通常のDQN更新式 `Q(s,a) = r + γ * max Q(s',a')` がそのまま使える
- ゲーム終了時は、手を打ったプレイヤーと相手プレイヤーの両方に done=True で確定させる
- 相手側の pending には「中間報酬 + 終局報酬」を合算して返す

### 重要なバグ修正履歴
- **2026-03-16**: `r + γ*maxQ` → `r - γ*maxQ` に修正（当時は next_state が相手ターンだったため）
- **2026-03-17**: GameRunner を遅延コールバック方式に修正し、`r - γ*maxQ` → `r + γ*maxQ` に戻した（next_state が自分のターンになったため、通常の更新式が正しい）

### 中間報酬（シェーピング）設計（2026-03-16 改訂）

**旧設計（廃止）:**
- 2連=±0.05、3連=±0.10のみ
- 問題: 「相手の勝ち手を見逃した」ことへのペナルティがなく、防御を学べなかった

**旧設計（2026-03-16 廃止）:**
| 条件 | 報酬 |
|---|---|
| 自分の3連を作った | +0.30 |
| 自分の2連を作った | +0.10 |
| 相手の3連を防いだ | **+0.50** ← 攻撃より高かった |
| 相手の2連を防いだ | +0.10 |
| 相手の勝ち手を見逃した | -0.80 |

**問題:** Phase 1・2のほぼランダムな相手に対して防御報酬が攻撃報酬より大きいと、「無意味な防御（ランダムな相手は脅威にならない）」が「積極的な攻撃」より得になってしまい、攻撃を学ぶ動機が弱くなる。

**旧設計（2026-03-17 廃止）:**
| 条件 | 報酬 |
|---|---|
| 自分の3連を作った | +0.50 |
| 自分の2連を作った | +0.15 |
| 相手の3連を防いだ | +0.30 |
| 相手の2連を防いだ | +0.10 |
| 相手の勝ち手を見逃した | -0.80 |

**問題:** 勝敗報酬（±1.0）に対して中間報酬が大きすぎた。1ゲーム中に中間報酬が複数回発生するため、累積中間報酬が勝敗報酬を上回り、「勝つ」より「3連を作り続ける」方がQ値的に得になりうる。

**現行設計（2026-03-17 改訂）:**
| 条件 | 報酬 |
|---|---|
| 自分の3連を作った | +0.10 |
| 自分の2連を作った | +0.03 |
| 相手の3連を防いだ | +0.06 |
| 相手の2連を防いだ | +0.02 |
| 相手の勝ち手を見逃した | -0.15 |

**設計思想:**
- **勝敗報酬（±1.0）が圧倒的に大きいことが最重要** — 中間報酬はあくまで「ヒント」
- 攻撃 > 防御の比率は維持（攻撃が直接勝利に繋がるため）
- 盤面全スキャンではなく置いたマス起点のみなので高速

**フェーズ別報酬変更の検討と見送り理由:**
- Phase 1・2（弱い相手）は攻撃重視、Phase 3・4（強い相手）は防御重視、という切り替えも考えたが廃止
- ReplayBufferに前フェーズの経験が残るため、「報酬の意味が途中で変わる」と同じ盤面に対するQ値の正解が変わり学習が混乱する

---

## 過去の方針と廃止理由（失敗→原因→次の戦略）

このプロジェクトは7回の大きな方針転換を経ている。各失敗から得られた教訓が次の戦略の土台になっている。

### ステージ1・2（ランダムAI相手）→ 失敗

**何をしたか:** DQNをランダムAI相手に学習させた。ステージ2ではカリキュラム的に「過去の自分」を相手にした。

**何が起きたか:** ランダム相手で勝率79%を達成したが、ルールベースAI相手には**勝率0%**。

**推定原因:**
1. **ランダム相手では防御を学べない** — ランダムAIはでたらめに動くので、攻撃を防ぐ動機が生まれない。「適当に並べて4連を狙う」だけで勝てる。
2. **先手・後手の混在** — Connect Fourは先手必勝ゲーム。先手と後手では最適戦略が全く異なるのに、両方を同時に学習させた。
3. **「過去の自分」も弱い** — 弱い相手同士で学習しても強くならない。

**→ 教訓: 「目をつぶった相手に勝つ練習」では実力はつかない。相手には一定の戦略が必要。先手固定で学習すべき。**

### ステージ3（Self-play）→ 失敗

**何をしたか:** DQN同士を対戦させて自己改善を狙った。

**何が起きたか:** 破滅的忘却（Catastrophic Forgetting）が発生。
```
純Self-play: vs Random 77% → 44% に崩壊
```

**推定原因:**
- DQN同士が「対DQN戦略」に特化し、汎用的な戦略を忘れる。
- ランダムAIを混ぜても、「ランダムに勝つ能力」を維持するだけでは意味がない。

**→ 教訓: Self-playは相手の多様性を確保しないと崩壊する。弱い相手同士の学習は退化を招く。**

### ステージ4（vs ルールベースAI固定）→ 失敗

**何をしたか:** 完全なルールベースAI（noise=0.0）を相手に固定して学習。

**何が起きたか:**
```
ep7500: 33%（ベスト） → ep15000: 0% → ep17000: 19% → 不安定に推移
```

**推定原因:**
1. **ε=0の実力は0%だった** — 学習中の評価（ε=0.05混入）では33%に見えたが、純粋推論では0%。εによるランダム手が「まぐれ勝ち」を生んでいた。
2. **固定相手への過学習→崩壊サイクル** — ルールベースAIの「癖」に過学習した後に崩壊するパターンが繰り返された。
3. **相手が強すぎた** — ルールベースAI（vs ランダム勝率99%）は学習初期のDQNには難しすぎ、「負け(-1.0)ばかり」で何も学べなかった。

**→ 教訓: いきなり強い相手と戦わせても信号が薄すぎて学習できない。段階的に強くすべき。固定相手は過学習を招く。**

### ステージ5（NoisyRuleBasedカリキュラム + 大スケール中間報酬）→ 部分的成功 + 構造的バグ発見

**何をしたか:** NoisyRuleBasedAgent（noise=0.8→0.5→0.2）でカリキュラム学習。中間報酬（3連作成+0.50、勝ち手見逃し-0.80など）を導入。Phase 4でスナップショットプール方式を試みた。

**何が起きたか:**
- Phase 3（noise=0.2）では vs RuleBased 99.5% を達成（最高記録）
- しかし Phase 4 移行後に崩壊:
```
ph4移行直後: vs RuleBased 86〜87% → ep3000〜: 18〜58%（急落） → ep5000〜: 5〜11%（崩壊）
```

**推定原因（3つ、うち2つは構造的バグ）:**

1. **Phase 4のプール方式が本質的に問題** — スナップショット（過去の自分）は常にDQNより弱い。弱い相手に勝てる「攻撃的すぎる戦略」が強化され、対強敵戦略が崩れた。ステージ3のself-play失敗と同じ構造。

2. **【構造的バグ】next_stateが「相手のターンの状態」だった** — GameRunnerが `on_step_end` を「自分が打った直後」に呼んでおり、`next_state` は相手のターンの盤面だった。Q学習では `max Q(s',a')` を計算するが、ネットワークは「自分のターンの状態」で学習しているため、「相手のターンの状態」を入力した予測は信頼できない。対症療法として `r - γ*maxQ(s')` と符号反転していたが、本質的には不正確。Phase 3までは相手の20%ランダム性がこの不正確さを吸収していたが、Phase 4（ほぼ完全ルールベース）ではQ値の不正確さが致命的になった可能性が高い。

3. **【構造的バグ】中間報酬が大きすぎた** — 勝敗報酬（±1.0）に対して中間報酬（3連作成+0.50、見逃し-0.80）が大きすぎた。1ゲーム中に中間報酬が複数回発生するため、累積中間報酬が勝敗報酬を上回りうる。すると「勝つ」より「3連を作り続ける」方がQ値的に得になり、最終盤の勝ち手を逃す行動が強化されうる。

**→ 教訓: two-player ゲームでは next_state の視点が重要。中間報酬は勝敗報酬を超えてはならない。**

### ステージ6（遅延コールバック + 小スケール中間報酬 + 目標80% + プール方式Phase4）→ 方針修正

**何をしたか:** ステージ5のバグ2つを修正（遅延コールバック + 中間報酬縮小）してゼロから再学習。Phase 1目標80%、Phase 4はスナップショットプール方式を維持。

**何が起きたか（Phase 1, ep25500時点）:**
```
vs Noisy(0.8): 73〜80%で振動（目標80%に対して1回Clearのみ、2回連続クリアに到達できず）
vs RuleBased: ベスト24.5%（ep22000）
ε: 0.10（収束済み）
ep19000で1回Clear → その後6500ep（約36分）2回目のClearなし
```

**推定原因:**
1. **Phase 1 目標80%が高すぎた** — noise=0.8（80%ランダム）相手の勝率は73〜80%で自然に振動する。2回連続80%超えは運の要素が大きく、学習の質と関係なく足踏みしてしまう。
2. **Phase 4 プール方式は不要** — ステージ5で既に失敗が確認されており、維持する理由がなかった。

**→ 教訓: ほぼランダムな相手での目標は控えめに。早く次のフェーズに進んで「ルールに従う手」への対応力を磨く方が学びが大きい。スナップショットプール方式は弱い相手との対戦で学習を退化させるため廃止。**

### ステージ7（現行: シンプルカリキュラム + 完全RuleBased最終フェーズ）← 学習中

**ステージ6の方針を修正:**

1. **Phase 1 目標 80% → 75%**: noise=0.8での足踏み時間を削減
2. **Phase 4 スナップショットプール廃止**: 純粋に noise=0.1 のNoisyRuleBasedのみ
3. **Phase 5 追加**: noise=0.0（完全ルールベースAI）、目標勝率40%

**Phase 5（完全RuleBased）を試す根拠:**
- 過去（ステージ4）での失敗原因は (1) next_stateバグ (2) いきなり強い相手 の2つ
- ステージ7では (1) 遅延コールバックで修正済み (2) Phase 1〜4で段階的に鍛えてから挑む
- 固定相手への過学習→崩壊が起きたら打ち切り判断が可能

**追加の設計判断（ステージ6から継続）:**
- **ReplayBufferはフェーズ昇格時にクリアしない**: 中間報酬が小さいので経験の意味が変わらない。古い経験は capacity=200000 で自然に押し出される。
- **スナップショットは2系統**: `best_ep*`（vs RuleBasedベスト更新時）+ `phaseup_ph*`（フェーズ昇格時）
- **チェックポイント保存**: ReplayBuffer + ε + Adam状態を `_checkpoint.npz` に保存・復元可能

---

## 現行方針の論拠

**「まず攻め方を覚えてから、防ぎ方を覚える」**

人間も将棋・囲碁を覚えるとき、段階的に強くなる。DQNも同様に、いきなり強い相手（ルールベースAI固定）と戦わせても信号が薄すぎて学習できない。

### NoisyRuleBasedAgent（段階的カリキュラム）

`noise`パラメータで相手の強さを連続的に調整できる：
```python
# noise=1.0 → ランダムAIと同じ
# noise=0.0 → 完全なルールベースAI
# noise=0.3 → 70%はルールに従い、30%はランダム
```

カリキュラム設計（昇格条件: vs Noisy 500戦 × 2回連続クリア）:
| フェーズ | 相手 | 目標勝率 |
|---|---|---|
| 1 | NoisyRuleBased(noise=0.8) | 75% |
| 2 | NoisyRuleBased(noise=0.5) | 70% |
| 3 | NoisyRuleBased(noise=0.2) | 60% |
| 4 | NoisyRuleBased(noise=0.1) | 50% |
| 5 | RuleBasedAgent(noise=0.0) | 40% |

**昇格条件の設計思想:**
- 旧設計（vs Noisy 200戦 1回クリア）ではまぐれ昇格が発生（ph3を1500epで通過）
- 500戦にすることで振れ幅を±7% → ±4%程度に縮小
- 2回連続クリアを要求することで「一時的な上振れ」での昇格を防ぐ

**Phase 5（完全RuleBased）の設計思想:**
- 過去（ステージ4）で完全RuleBased固定は失敗したが、原因は (1) next_stateバグ (2) 段階的強化なし の2つ
- ステージ7では両方が解消されている（遅延コールバック + Phase 1〜4で段階的に鍛えてから挑む）
- 崩壊パターン（勝率上昇後に急落）が見えたら打ち切り判断が可能

**Phase 4 スナップショットプール方式の廃止理由（2026-03-17）:**
- ステージ5で既に失敗確認済み（ph4移行後に86% → 5%に崩壊）
- 弱いスナップショット（過去の自分）との対戦が学習を退化させる（ステージ3のself-play失敗と同構造）
- シンプルに noise を下げていく方式に統一

### 攻防セット中間報酬の狙い

- 攻撃報酬（自分の2連・3連）→ 積極的に4連を狙う動機
- 防御報酬（相手の2連・3連を防いだ）→ 妨害する動機
- 防御失敗ペナルティ（相手の勝ち手を見逃した）→ 防御の失敗に即座に気づかせる

攻撃 > 防御に設定することで「負けないけど勝てない（守備偏重）」を防ぐ。

### 中間報酬はルールベースAIを手書きで教えているのか？

ある程度そうだが、**目的は基礎を身につけること**。基礎を身につけた後に、DQNはルールベースAIを超える戦略（複数の脅威を同時に作るフォークなど）を自然に学べる可能性がある。

---

## 現在の方針（2026-03-17 更新）

**ステージ7: シンプルカリキュラム学習（Phase 1 から再学習中）**

ステージ6の目標・構造を修正し、ステージ6のベストモデル（ep22000, vs RuleBased 24.5%）を初期値として再学習:

```bash
# 現在実行中（ステージ6ベストモデルを初期値にPhase 1から）
.venv/Scripts/python train.py --load-path weights/snapshots/best_ep22000_rb24pct --no-buffer --episodes 50000

# ゼロから学習する場合
.venv/Scripts/python train.py --episodes 50000

# 中断後の再開
.venv/Scripts/python train.py --load-path weights/dqn_connect4 --episodes 50000
```

## 今後の展望

1. **Phase 1〜4 を順次クリア**（カリキュラムで段階的に強化）
2. **Phase 5（完全RuleBased）での挙動を観察**（崩壊するか、安定して学べるか）
3. **vs RuleBased 勝率の推移を監視**（Phase 5で過学習→崩壊パターンが出たら打ち切り）
4. **最終目標: 初心者の人間に勝率50%以上**（人間と対戦して確認）
